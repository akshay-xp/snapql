---
name: grill-with-docs
description: A relentless interview to sharpen a plan or design, which also creates docs (ADR's and glossary) as we go. Only invoke when the user explicitly calls this via slash command.

---

Run a `/grilling` session, using the `/domain-modeling` skill.
